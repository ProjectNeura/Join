{
  "name": "projectneura-join",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --local",
    "deploy": "wrangler pages deploy public --project-name projectneura-join",
    "db:migrate:local": "wrangler d1 migrations apply projectneura-join --local",
    "db:migrate:remote": "wrangler d1 migrations apply projectneura-join --remote"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
